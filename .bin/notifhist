end=$((SECONDS+1)) # Set time

while [ $SECONDS -lt $end ]; do # If seconds does not equal end time
 dunstctl history-pop # Show dunst history
 :
done

sleep 2 # Wait

dunstctl close-all # Close all notifications
