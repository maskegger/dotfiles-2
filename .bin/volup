pulsemixer --change-volume +5 # Volume up
dunstctl close-all # Close all notifications
dunstify "Volume Up" "`~/.bin/vol`" -a Volume -i /usr/share/icons/Papirus/16x16/actions/audio-volume-high.svg # Send notification
