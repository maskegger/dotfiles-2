# Services
sudo systemctl enable touchegg sddm bluetooth NetworkManager wpa_supplicant

systemctl enable --user syncthing

# Directories
mkdir ~/Pictures/Screenshots

# Time
timedatectl set-timezone Asia/Dubai
