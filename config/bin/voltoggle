pactl set-sink-mute 0 toggle

if [[ $(pamixer --get-mute) == "true" ]]; then
  notify-send 'System notification' "Audio has been muted"
else
  notify-send 'System notification' "Audio has been unmuted"
fi
